{
   "name": "workshop-react",
   "version": "1.0.0",
   "private": true,
   "workspaces": [
      "front",
      "back"
   ],
   "scripts": {
      "back:dev": "cd back && yarn dev",
      "front:dev": "cd front && yarn dev",
      "front:test": "cd front && yarn test:dev",
      "dev": "run-p back:dev front:dev"
   },
   "devDependencies": {
      "npm-run-all": "^4.1.5"
   }
}