import { makeStyles } from "@material-ui/core";

export const useSquadFilterStyles = makeStyles({
  root: {
    marginTop: 10,
    marginBottom: 10,
  },
  checkBox: {
    transform: "scale(1.5)",
  },
});
